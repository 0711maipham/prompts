import React, { useRef, useState } from "react"
import { Card, Button, Form, Alert } from 'react-bootstrap'

export default function SavedDecks() {
    return(
        <Card>
            <Card.Body>
            <h2 className="text-center mb-4">Saved Pods</h2>
            <p>
                Coming Soon.
            </p>
            </Card.Body>
        </Card>
    )
}